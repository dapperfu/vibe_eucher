# GPU Training Requirements for M-Series AI Models
# These packages provide CUDA support for PyTorch training

# Core PyTorch with CUDA support
# Note: Install PyTorch with CUDA using the index URL instead:
# pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
torch>=2.0.0
torchvision>=0.15.0
torchaudio>=2.0.0

# Additional PyTorch ecosystem
pytorch-lightning>=2.0.0
torchmetrics>=1.0.0

# GPU monitoring and optimization
nvidia-ml-py3>=7.352.0
pynvml>=11.5.0

# Scientific computing
numpy>=1.24.0
scipy>=1.10.0

# Data processing
pandas>=2.0.0
scikit-learn>=1.3.0

# Progress bars and utilities
tqdm>=4.65.0
rich>=13.0.0

# Logging and configuration
wandb>=0.15.0
tensorboard>=2.13.0

# Optional: For advanced GPU features
# apex  # NVIDIA Apex for mixed precision training
# deepspeed  # Microsoft DeepSpeed for distributed training 